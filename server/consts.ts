export const APPLICATION = {
    PORT: 8080,
    URL: 'localhost',
}

export const BASEURL = `${APPLICATION.URL}:${APPLICATION.PORT}`;

export const ENDPOINTS = {
    SECURE: `/secure/`,
    INSECURE: `/insecure/`,
}
